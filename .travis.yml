language: python

python:
  - "2.7"
  - "3.3"

install:
  - pip install codecov
  - pip install coverage

script: 
  - python -m unittest discover -v ./
  - coverage run ./mytests.py

# codecov runs after travis ci tests succeed
after_success:
  - codecov